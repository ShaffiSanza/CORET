# CORET Backend — Python-avhengigheter
# Installer med: pip install -r requirements.txt

# --- Web-rammeverk ---
fastapi>=0.115.0              # API-rammeverket (håndterer HTTP, validering, docs)
uvicorn[standard]>=0.30.0     # ASGI-server (kjorer FastAPI-appen)

# --- HTTP-klient ---
httpx>=0.27.0                 # Async HTTP-klient (for å kalle eksterne APIer)

# --- Konfigurasjon ---
python-dotenv>=1.0.0          # Leser .env-filer
pydantic>=2.9.0               # Datavalidering (brukes av FastAPI)
pydantic-settings>=2.5.0      # Laster config fra .env

# --- Bildebehandling ---
colorthief>=0.2.1             # Henter dominerende farge fra bilder
Pillow>=10.0.0                # Bildebibliotek (brukes av colorthief)
rembg[cpu]>=2.0.50            # Bakgrunnsfjerning med U2-Net (lokal, ingen API)

# --- Fileopplasting ---
python-multipart>=0.0.12      # Nodvendig for å motta filopplastinger i FastAPI

# --- Monitoring ---
sentry-sdk>=2.0.0             # Error tracking + performance monitoring (Sentry)

# --- Testing ---
pytest>=8.0.0                 # Test-rammeverk
pytest-asyncio>=0.24.0        # Async-stotte for pytest
respx>=0.21.0                 # Mocker httpx-kall i tester (som unittest.mock for HTTP)
